<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.com.example.android.smartstudykanji">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectLevelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DisplayActivity"
            android:label="@string/display_menu" />
        <activity
            android:name=".SelectUnitN4"
            android:label="@string/select_unit" />
        <activity android:name=".SelectUnitN3"
            android:label="@string/select_unit"/>
        <activity
            android:name=".ProgressActivity"
            android:label="@string/display_progress" />
        <activity android:name=".FeedBack" />
        <activity
            android:name=".AboutUs"
            android:label="@string/title_activity_about_us"
            />
    </application>

</manifest>